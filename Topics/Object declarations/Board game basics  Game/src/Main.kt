
object PlayingField {
    object Size {
        var width: Int = 2
        var height: Int = 2
    }

    fun changeSize(newWidth: Int, newHeight: Int) {
        var newWidth = Size.width
        var newHeight = Size.height
    }
}
